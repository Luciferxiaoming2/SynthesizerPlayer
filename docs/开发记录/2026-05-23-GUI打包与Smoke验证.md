# 2026-05-23 GUI 打包与 Smoke 验证

## 本次目标

在命令行 exe 已经可构建的基础上，补齐 PyQt6/QML 图形界面 MVP 的 Windows 打包入口，并完成一次打包后 smoke 验证。

## 已完成

- 扩展 `ui/main_window.py`：
  - 新增 `AUDIO_FORGE_UI_SMOKE=1` smoke 模式，QML 加载成功后立即退出。
  - frozen 环境下使用 `sys._MEIPASS` 定位 QML 资源。
  - frozen 环境下将用户工作根目录设为 exe 所在目录，避免把 mock/export 写入 `_internal`。
  - QML 加载失败时输出诊断信息。
- 新增 `scripts/packaging/audio_forge_ui.spec`：
  - 打包 `audio-forge-ui.exe`。
  - 纳入 `ui/qml` 资源。
  - 保留 PyQt6/QML、sounddevice、soundfile、numpy、pedalboard 所需依赖。
- 新增 `scripts/packaging/build_windows_ui.ps1`。
- 更新 `Makefile`：
  - 新增 `package-ui`。
- 更新 `docs/runbooks/packaging_windows.md`：
  - 记录 GUI 打包命令和 smoke 自测方式。

## 本机验证

- 已成功构建：`dist/audio-forge-ui/audio-forge-ui.exe`。
- 已通过 frozen smoke：
  - 环境变量：`QT_QPA_PLATFORM=offscreen`、`QSG_RHI_BACKEND=software`、`QT_QUICK_CONTROLS_STYLE=Basic`、`AUDIO_FORGE_UI_SMOKE=1`。
  - 输出：`Audio Forge UI smoke loaded`。

## 当前边界

- 还没有安装器、图标、版本信息、代码签名。
- GUI 真实声卡播放还需要人工试听。
- 打包体积后续需要评估，模型和 VST 仍不默认打包。
