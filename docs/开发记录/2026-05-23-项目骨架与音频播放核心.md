# 2026-05-23 开发记录

## 1. 项目骨架与 Harness 分层

完成内容：

- 建立 `core_engine/`、`harness/`、`ui/`、`plugins/` 分层目录。
- 建立 `pyproject.toml`、`Makefile`、`.env.example`、`.gitignore`。
- 建立 `HARNESS.md`、`docs/harness/`、`.agent/`、`scripts/`，把通用 AI Agent Harness 思想落到项目治理层。
- 新增架构说明、模块规格和 ADR，明确 `core_engine` 不依赖 UI，`harness` 先验证底层能力。

验证：

- 使用 `D:\uv\venvs\audio_forge\Scripts\python.exe scripts\run_agent_checks.py` 验证通过。

## 2. 第一阶段：无 UI 音频核心与评估脚手架

完成内容：

- 实现双轨音频读取、声道匹配、帧对齐、混音和写出。
- 实现互相关音频延迟评估。
- 实现轻量级跑调 fallback，用分块局部漂移保持总时长不变。
- 新增 mock 音频生成、双轨离线混音、跑调渲染 CLI。
- 新增 pytest 测试覆盖核心 buffer、延迟评估、跑调 fallback 和 mock 音频。

验证：

- `D:\uv\venvs\audio_forge\Scripts\python.exe -m pytest -q`：8 passed。
- `D:\uv\venvs\audio_forge\Scripts\python.exe -m harness.cli_harness.generate_mock_audio --output-dir harness\mock_data`：生成 mock 双轨音频。
- `D:\uv\venvs\audio_forge\Scripts\python.exe -m harness.cli_harness.run_dual_mix --vocal harness\mock_data\vocal.wav --instrumental harness\mock_data\instrumental.wav --output harness\mock_data\mix.wav`：生成离线混音。
- `D:\uv\venvs\audio_forge\Scripts\python.exe -m harness.cli_harness.run_pitch_shift --input harness\mock_data\vocal.wav --output harness\mock_data\vocal_tone_deaf.wav --ratio 0.4`：生成跑调人声。
- `D:\uv\venvs\audio_forge\Scripts\python.exe scripts\run_agent_checks.py`：Agent checks passed。

## 3. 当前下一步

计划继续：

- 实现实时播放核心状态机和 callback 渲染器。
- 将 `sounddevice` 声卡输出做成可选适配器，避免测试依赖真实声卡。
- 增加播放 seek、gain、mute/solo 基础能力。

## 4. 第二阶段：播放核心状态机

完成内容：

- 新增 `core_engine/player/playback_engine.py`，实现 `DualTrackPlaybackEngine`。
- 支持播放、暂停、停止、seek、播放位置查询。
- 支持人声/伴奏 gain、mute、solo。
- `render_block(frame_count)` 可作为音频 callback 的底层渲染接口，测试时不需要真实声卡。
- 新增 `core_engine/player/sounddevice_output.py`，把 `sounddevice` 作为可选输出适配器，只有实际播放时才导入。
- 新增 `harness/cli_harness/run_playback.py`，后续安装 `sounddevice` 后可直接播放 mock 双轨。

验证计划：

- 运行 pytest 验证状态机和渲染块行为。
- 运行 agent checks 验证架构分层没有破坏。

验证结果：

- `D:\uv\venvs\audio_forge\Scripts\python.exe -m pytest -q`：12 passed。
- `D:\uv\venvs\audio_forge\Scripts\python.exe scripts\run_agent_checks.py`：Agent checks passed。
- 已安装 `sounddevice==0.5.5`。
- `sounddevice.query_devices()` 查询成功，共发现 31 个音频设备；默认输出设备显示为 Realtek 耳机。
- 未自动执行真实播放，避免未经确认直接让电脑出声。

## 5. 第三阶段：实时播放控制层

完成内容：

- 将 `DualTrackPlaybackEngine` 改造成线程安全状态机，内部使用锁保护播放位置、播放状态和控制参数。
- 新增 `PlaybackSnapshot`，用于 UI 或 CLI 读取一致的播放状态快照。
- 新增便捷控制方法：
  - `set_gains`
  - `set_mute`
  - `set_solo`
  - `snapshot`
- 为播放 CLI 增加交互控制命令：
  - `status`
  - `play`
  - `pause`
  - `stop`
  - `seek <seconds>`
  - `gain <vocal|instrumental> <value>`
  - `mute <vocal|instrumental> <on|off>`
  - `solo <vocal|instrumental> <on|off>`
  - `quit`
- 新增 CLI 命令解析测试，确保后续 UI 绑定前先在命令行验证控制语义。

验证：

- `D:\uv\venvs\audio_forge\Scripts\python.exe -m pytest -q`：17 passed。
- `D:\uv\venvs\audio_forge\Scripts\python.exe scripts\run_agent_checks.py`：Agent checks passed。

说明：

- 真实播放命令已经具备交互控制能力，但仍建议手动确认电脑音量和输出设备后再运行。
