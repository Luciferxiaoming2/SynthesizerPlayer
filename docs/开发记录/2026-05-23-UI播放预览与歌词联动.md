# 2026-05-23 UI 播放预览与歌词联动

## 本次目标

继续迁移旧源代码中的播放交互语义：播放/暂停/停止、进度拖动、音量调节、歌词高亮。当前阶段先接入 UI 可验证的播放预览状态机，不直接占用声卡输出。

## 已完成

- 扩展 `ui/main_window.py`：
  - `WorkbenchBridge` 新增双轨加载。
  - 新增播放预览状态：`play`、`pause`、`stop`、`advancePlayback`。
  - 新增 `seekProgress`，用于 UI 进度条拖动。
  - 新增 `setTrackGains`，用于 vocal/instrumental 音量控制。
  - 新增歌词路径、当前歌词、下一句歌词属性。
  - mock 生成时同步写入一份测试 LRC，方便 UI 直接验证歌词联动。
- 扩展 `ui/qml/SingleScreenUI.qml`：
  - 增加歌词路径输入。
  - 增加 Load、Play/Pause、Stop。
  - 增加播放进度条和时间显示。
  - 增加 vocal/instrumental gain 双滑块。
  - 增加当前歌词和下一句显示。
- 扩展 `tests/test_ui_workbench.py`：
  - 覆盖 UI bridge 的播放推进、seek、暂停、停止和歌词状态。

## 本机验证

- `tests/test_ui_workbench.py` 通过。
- QML offscreen 加载 root object 成功，且无 QML binding warning。

## 对旧源代码迁移状态的影响

- 歌词高亮业务语义：已迁移第一版。
- 进度条拖动业务语义：已迁移第一版。
- 音量调节业务语义：已迁移第一版。
- 真实声卡播放 UI：还未接入，下一步应将 `SoundDeviceOutput` 与 UI 状态安全连接。

## 下一步建议

接入真实音频输出开关，同时保持 harness 可测试；随后补文件选择弹窗和 GUI 打包目标。
