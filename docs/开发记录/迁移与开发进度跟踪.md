# 迁移与开发进度跟踪

## 目标

以 `源代码/` 旧项目作为功能参考，选择性吸收业务能力到当前新架构中。旧目录保持不变，不直接修改、不原样搬运 VLC/ffmpeg/歌曲资源/旧 PyQt5 UI。

当前新架构目标：

- `core_engine/`：无 UI 的播放、歌词、DSP、VST、AI 适配逻辑。
- `harness/`：命令行与评估脚手架。
- `ui/`：后续 QML/PyQt6 单屏界面。
- `docs/开发记录/`：记录每批迁移与开发动作。

## 迁移原则

- 旧代码只读参考，所有新增能力写入新项目结构。
- 优先迁移“业务语义”和“数据模型”，不迁移旧 UI 形态。
- 每批迁移后必须跑测试和 `agent-checks`。
- 每批迁移后更新本文档状态。
- 重型二进制和音频资源不进入新源码管理。

## 总体进度

| 阶段 | 内容 | 状态 | 预计耗时 | 目标位置 | 验收方式 |
|---|---|---:|---:|---|---|
| M0 | 项目骨架与 Harness 分层 | 已完成 | 已完成 | 全项目 | `agent-checks` 通过 |
| M1 | 歌词解析、offset、歌曲扫描 | 已完成 | 已完成 | `core_engine/lyrics`, `core_engine/library` | 旧 songs 目录扫描成功，pytest 通过 |
| M2 | 播放位置与歌词高亮联动 | 已完成 | 0.5 天 | `core_engine/lyrics`, `ui/viewmodels` 或 harness | 给定播放时间返回当前歌词行 |
| M3 | 歌曲选择 / 导入流程重建 | 已完成 | 0.5-1 天 | `core_engine/library`, `harness/cli_harness`, `ui` | 可从目录导入/选择一首双轨歌曲 |
| M4 | 进度拖动、音量、静音、solo 语义补齐 | 已完成第一版 | 0.5 天 | `core_engine/player`, `ui` | 播放状态机测试覆盖，UI 可 seek 和调整增益 |
| M5 | 跑调开关与强度参数接入新 F0 pipeline | 已完成第一版 | 0.5-1 天 | `core_engine/dsp`, `harness/cli_harness`, `ui` | 可渲染不同强度跑调并评估 |
| M6 | 跑调缓存与播放 buffer 替换 | 已完成 | 1 天 | `core_engine/player`, `core_engine/dsp` | 离线渲染结果可被播放引擎切换 |
| M7 | VST 离线处理 MVP | 已完成 | 1-2 天 | `core_engine/dsp/vst_host.py`, `harness/cli_harness` | 可选择插件路径并离线渲染 |
| M8 | 导出处理后音频 | 已完成 | 0.5-1 天 | `harness/cli_harness`, 后续 UI | 输出 wav，后续接 ffmpeg mp3 |
| M9 | 改词唱实验性接口 | 已完成第一版 | 1-2 天 | `core_engine/ai_singer`, `harness/cli_harness` | preview 后端可生成 wav，外部 DiffSinger/RVC 可通过命令适配器接入 |
| M10 | PyInstaller 打包准备 | 已完成第一版 | 2-4 天 | `scripts/packaging`, docs | CLI 和 GUI MVP 均具备 PyInstaller 打包入口 |
| M11 | PyQt6/QML 单屏 UI MVP | 进行中，导入后台任务化完成 | 2-4 天 | `ui/main_window.py`, `ui/qml` | UI 可启动，支持歌曲库扫描、文件选择、mock、预览播放、音频设备选择、真实音频输出入口、歌词、导出 |
| M12 | UI 评估与诊断入口 | 进行中 | 1-2 天 | `harness/eval_harness`, `ui` | UI 可触发同步延迟评估 |
| M13 | 单曲导入自动拆解 | 进行中，工程入口第一版完成 | 3-5 天 | `core_engine/importer`, `harness/cli_harness`, `ui` | 导入完整歌曲后自动生成人声、伴奏和歌词时间轴 |
| M14 | ffmpeg/rubberband 可选外部后端 | 进行中，本地工具发现和 ffmpeg 标准化已跑通 | 1-2 天 | `core_engine/external_tools`, `harness/cli_harness` | 可探测工具，可选使用 ffmpeg 标准化导入音频 |
| M15 | 右栏功能面板真实化 | 进行中 | 1-2 天 | `ui/main_window.py`, `ui/qml` | 右栏不再显示假数据，预设/诊断/状态能真实反映当前工程 |

## 旧源代码功能映射

| 旧代码功能 | 旧位置 | 新项目处理方式 | 状态 |
|---|---|---|---:|
| LRC 解析 | `play.py` / `LyricsLoader` | 重建为 `core_engine/lyrics/parsers.py` | 已完成 |
| SRT 解析 | `play.py` / `SRTLoader` | 重建为 `core_engine/lyrics/parsers.py` | 已完成 |
| offset 保存 | `offsets.json`, `OffsetManager` | 重建为 `core_engine/library/offsets.py` | 已完成 |
| 歌曲目录扫描 | `SongManager.scan` | 重建为 `core_engine/library/song_scanner.py`，并接入 UI 歌曲库扫描 | 已完成 |
| 双播放器同步 | `AudioPlayer` + pyglet | 不迁移，已用 `DualTrackPlaybackEngine` + `SoundDeviceOutput` 重建 | 已完成第一版 |
| 歌词高亮 | `LyricsScrollArea.highlight_line` | 迁移为 UI 无关的时间轴状态模型，并接入 QML 当前/下一句显示 | 已完成第一版 |
| 进度条拖动 | `on_slider_*` | 接入 `DualTrackPlaybackEngine.seek_seconds` 和 QML 进度 slider | 已完成第一版 |
| 音量/静音 | `change_*_volume`, `toggle_*_mute` | 已有 `TrackControls`，QML 已接入 vocal/instrumental gain | 已完成第一版 |
| 跑调开关 | `RealisticVocalProcessor` | 不原样迁移，改接新 F0 pipeline，并接入导出 slider | 已完成第一版 |
| ffmpeg/rubberband | `ffmpeg/`, `RealisticVocalProcessor` | 不迁移旧二进制，保留为可选外部工具后端 | 进行中 |
| PyQt5 UI | `MainWindow` | 不原样迁移，后续用 PyQt6/QML 重建 | 不迁移 |
| VLC 目录 | `VLC/` | 不迁移 | 不迁移 |
| 歌曲 mp3 资源 | `songs/` | 只作为本地测试参考，不纳入新源码 | 不迁移 |

## 当前已完成验证

- `D:\uv\venvs\audio_forge\Scripts\python.exe -m pytest -q`：42 passed。
- `D:\uv\venvs\audio_forge\Scripts\python.exe scripts\run_agent_checks.py`：Agent checks passed。
- `scan_songs` 可扫描旧 `songs` 目录，识别 3 首双轨歌曲。
- `export_mix` 可输出 `harness/mock_data/export_mix.wav`。
- `run_lyric_ai` 已具备 preview 改词唱占位音频生成入口，真实模型保持外部后端适配。
- `dist/audio-forge-cli/audio-forge-cli.exe` 已完成一次本机 PyInstaller 构建，并通过 `generate-mock`、`export-mix`、`run-lyric` 最小自测。
- `dist/audio-forge-ui/audio-forge-ui.exe` 已完成一次本机 PyInstaller 构建，并通过 frozen smoke 验证。
- QML 单屏 UI 已通过 offscreen 加载检查，`WorkbenchBridge` 可生成 mock、播放预览推进、真实音频输出入口、歌词状态更新和导出 wav。
- UI 已支持 vocal、instrumental、lyrics 和 output 的文件选择入口。
- UI 已支持选择 songs 根目录、扫描歌曲、从 ComboBox 选择歌曲并自动加载双轨和歌词。
- UI 已支持刷新并选择 SoundDevice 输出设备。
- UI 已支持触发 vocal/instrumental 同步延迟评估。
- 已新增 ffmpeg/rubberband 外部工具探测；ffmpeg 可选标准化导入音频入口已完成。
- 外部工具探测已支持项目本地和旧源码目录，`import_song --standardize-audio` 已验证可用本地 ffmpeg 生成 `original_standard.wav`。
- Demucs 导入后端已支持注入项目本地 `TORCH_HOME` 与 `ffmpeg/ffprobe` 目录，并新增 soundfile 保存 runner 以绕开 Windows `torchcodec` 保存链路；本机真实端到端导入已成功生成 vocal/instrumental stems。
- 导入工程 manifest 已记录分离后端、歌词后端、是否标准化和是否复制原曲；UI 导入完成状态已显示所用后端。
- UI 导入入口已改为后台任务，导入中显示 busy 状态并禁用重复提交。
- GUI PyInstaller 包已重新构建并通过 offscreen smoke；Demucs runner 作为 data 文件随包放入 `_internal`，避免把 torch 打进 GUI 主包。

## 下一批开发计划

### P0：客户侧单曲导入自动拆解

目标：

- 用户只导入一首完整歌曲文件，不要求提前准备 vocal/instrumental 双轨。
- 系统自动创建歌曲工程目录。
- 调用人声分离后端生成 `vocal.wav` 和 `instrumental.wav`。
- 优先匹配同目录 `.lrc` / `.srt`；缺失时进入 ASR 歌词识别/转写流程。
- 分离完成后自动加载到现有播放、歌词、跑调、VST、导出链路。

状态：进行中，工程入口第一版完成。

目标文件：

- `core_engine/importer/`
- `core_engine/player/stem_separator.py`
- `core_engine/lyrics/` 或后续 `core_engine/transcription/`
- `harness/cli_harness/import_song.py`
- `ui/main_window.py`

验收：

- 给定一首完整歌曲，命令行可生成工程目录和双轨 stem。已完成第一版。
- UI 可通过“导入歌曲”触发处理并进入可播放状态。已完成第一版。
- 没有歌词文件时，系统能给出明确状态：自动转写中、转写成功或需要用户补充歌词。preview 占位转写已完成，faster-whisper 可选 ASR adapter 已接入。

完成说明：

- 新增 `core_engine/importer/song_importer.py`，负责创建工程目录、复制原曲、调用分离后端、匹配歌词和写入 manifest。
- `project.json` 已记录导入后端、歌词后端、音频标准化状态和原曲复制状态。
- 新增 `PreviewStemSeparator`，用于在未配置 Demucs/Spleeter/UVR5 时跑通工程链路。
- 新增 `ExternalCommandStemSeparator`，为真实分离器预留命令模板接口。
- 新增 `DemucsStemSeparator`，默认 CPU 模式调用 `python -m demucs` 并归一化输出文件名。
- `DemucsStemSeparator` 已支持注入项目本地 `TORCH_HOME` 与 `ffmpeg/ffprobe` 目录，避免依赖用户缓存和系统 PATH。
- 新增 Demucs soundfile runner，保留开源 Demucs 推理，wav 保存复用项目已有 `soundfile`。
- 新增 `harness/cli_harness/import_song.py`。
- UI 新增 `Import Song` 入口。
- 新增 `core_engine/transcription`，支持 preview 歌词生成和外部 ASR 命令适配。
- 新增 `FasterWhisperLyricsTranscriber`，作为可选本地 ASR 后端。
- UI 导入歌曲时已支持选择分离后端 `preview/demucs` 与歌词后端 `preview/faster-whisper/none`。
- UI 导入歌曲已使用 `QThread` 后台任务，避免 Demucs 长耗时导入阻塞主界面。

### P1：播放位置与歌词状态联动

目标：

- 给定播放位置毫秒数和 offset，返回当前歌词行。
- 暴露一个 UI 可读的状态对象：当前行、上一行、下一行、当前文本、进度。

状态：已完成。

目标文件：

- `core_engine/lyrics/timeline.py`
- `core_engine/lyrics/playback_sync.py`
- `tests/test_lyrics_playback_sync.py`

验收：

- pytest 覆盖 offset、边界时间、空歌词。
- 不依赖 UI。

完成说明：

- 新增 `LyricPlaybackSynchronizer` 和 `LyricPlaybackState`。
- 新增 `harness/cli_harness/lyrics_at.py` 用于命令行验证当前歌词行。
- 已覆盖 offset、SRT end time、空歌词、前后行和行内进度。

### P2：歌曲导入/选择流程

目标：

- 从目录扫描歌曲。
- 选择歌曲后返回 vocal/instrumental/lyrics/offset 的统一对象。

状态：已完成。

目标文件：

- `core_engine/library/song_session.py`
- `harness/cli_harness/load_song_session.py`
- `tests/test_song_session.py`

验收：

- 能加载旧 songs 目录中的歌曲元数据。
- 不读取或复制音频内容，只建立路径和歌词状态。

完成说明：

- 新增 `SongSession`。
- 新增 `select_song_by_name`。
- 新增 `load_song_session` CLI。
- 歌曲会被统一加载为资源路径、歌词时间轴和 offset。

### P3：跑调强度接入播放流程

目标：

- 将 `ToneDeafConfig.drift_ratio` 暴露为播放/导出参数。
- 为后续 UI slider 准备稳定接口。

目标文件：

- `core_engine/dsp/tone_deaf.py`
- `harness/cli_harness/run_pitch_shift.py`
- `harness/cli_harness/analyze_f0_drift.py`

验收：

- 不同 ratio 产生不同 drift 指标。
- 输出长度和 sample rate 保持不变。

完成说明：

- `ToneDeafConfig.drift_ratio` 已接入 CLI、缓存和导出流程。
- `export_mix` 可组合跑调、离线效果链和 master gain 输出 wav。

### P4：改词唱实验性接口

目标：

- 建立 UI 无关的“改词唱”任务对象。
- 默认不依赖重型模型，先用 preview 后端跑通流程。
- 预留 DiffSinger/RVC 等开源项目的外部命令适配器。

状态：已完成第一版。

目标文件：

- `core_engine/ai_singer/diff_singer_api.py`
- `core_engine/ai_singer/rvc_infer.py`
- `core_engine/ai_singer/workflow.py`
- `harness/cli_harness/run_lyric_ai.py`
- `tests/test_ai_singer_workflow.py`

验收：

- preview 模式可生成 wav。
- 可选 RVC 阶段能通过 bypass 或外部命令适配器串联。
- 不引入训练依赖，不要求普通用户本机具备 GPU。

### P5：Windows 打包准备

目标：

- 为普通用户无 Python 环境运行准备 PyInstaller 打包入口。
- 同时准备命令行 MVP 和 PyQt6/QML GUI MVP。
- 明确大模型、VST 和旧歌曲资源不默认进入包体。

状态：已完成第一版。

目标文件：

- `scripts/packaging/audio_forge_cli.py`
- `scripts/packaging/audio_forge_cli.spec`
- `scripts/packaging/audio_forge_ui.spec`
- `scripts/packaging/build_windows.ps1`
- `scripts/packaging/build_windows_ui.ps1`
- `docs/runbooks/packaging_windows.md`

验收：

- `audio_forge_cli.py` 可作为统一命令分发入口。
- PyInstaller spec 已排除 GUI 和重型实验依赖，降低基础包体风险。
- GUI spec 已纳入 QML 资源，并通过 smoke 验证。
- GUI 包已验证可包含 Demucs soundfile runner 脚本；真实 Demucs 仍走外部 Python/sidecar Python。
- runbook 写明构建和打包后自测命令。

### P6：PyQt6 单屏工作台

目标：

- 让 `audio-forge-ui` 不再是占位异常。
- 通过 QML 单屏界面触发 mock 生成和混音导出。
- 保持 UI 只调用桥接层，核心音频逻辑仍在 `core_engine`。

状态：进行中，第一版已完成。

目标文件：

- `ui/main_window.py`
- `ui/qml/SingleScreenUI.qml`
- `tests/test_ui_workbench.py`

验收：

- QML 可在 offscreen 模式创建 root object。
- `WorkbenchBridge` 可生成 mock 音频并导出 wav。
- `WorkbenchBridge` 可执行播放预览、暂停、停止、seek、歌词当前/下一句更新和 vocal/instrumental gain。
- 后续补文件选择和 GUI 打包。

## 风险记录

| 风险 | 影响 | 应对 |
|---|---|---|
| Intel 轻薄本不适合本地重 AI 推理 | 改词唱完整本地版不可控 | 一个月 MVP 只做接口/实验入口 |
| VST 插件兼容性差异大 | 实时 VST 容易崩溃 | 先做离线 VST 处理 |
| 旧代码编码显示异常 | 直接迁移会引入乱码和旧依赖 | 只迁移业务逻辑，重写新模块 |
| 打包体积过大 | 普通用户下载/运行困难 | AI 模型和 VST 不默认打包 |

## 2026-05-23 界面补充记录

- 已按截图方向将 QML 主界面调整为中文 K 歌软件风格：左侧歌曲库、中间滚动歌词、底部播放和音频控制区。
- 未完成或未接入的旧界面功能已置灰，包括搜索、人声静音、伴奏静音、真人唱功开关、歌词字体和歌词偏移。
- 跑调强度、人声、伴奏和主输出保留为可操作滑杆，导入歌曲仍走后台任务，避免 Demucs 长耗时阻塞界面。
- 人声静音和伴奏静音已接入播放引擎，不再置灰；主输出 VST 已接入离线导出入口，加载后在“导出”时生效。
- 歌词区已改为真正的滚动列表，播放时跟随当前歌词行；UI 导入非 wav 音频会自动用 ffmpeg 标准化，已用 `D:\music` 中 MP3/M4A 文件验证。
- 歌曲库扫描已支持普通音乐文件夹，不再只识别双轨工程目录；导入成功后会自动把新工程加入左侧列表并刷新歌词状态。
- 歌词后端状态已在 UI 展示；`faster-whisper` 未安装时会中文提示且不启动导入。左侧歌曲库已支持删除选中和清空列表，删除当前播放歌曲后自动切到下一首。
- 后端选择已改为中文显示，新增“生成歌词”按钮；无歌曲、选择“不生成歌词”、未安装本地识别依赖等情况都会给出中文前置条件提示。
- 跑调功能已增强并接入播放链路：滑杆会影响当前播放缓冲和导出结果，跑调漂移更明显，缓存已按音频指纹区分不同歌曲。
- 智能歌词识别已进入打包准备：开发环境安装了识别依赖，PyInstaller 会收集依赖和 `plugins/models/faster-whisper/base` 模型目录；UI 不再向用户暴露底层依赖名。
- 参考图播放器界面已完成第一版重构：左侧歌曲库、中间歌词主舞台、右侧 F0/DSP 面板和底部播放台；低频功能已收纳到右上角设置弹窗。
- UI 卡顿与缩放问题已完成第一轮修复：歌词列表不再随播放定时器反复重建，跑调拖动改为合并应用，主界面按设计稿比例随窗口同步缩放。
- 跑调爆音已做软限幅与能量控制；“生成歌词”按钮回到歌词主舞台，导入无歌词歌曲时会弹窗询问是否立即生成歌词。
- 歌词生成已后台化：生成时显示进度条和阶段提示，ASR 不再卡住界面；生成文本会尽量保持简体中文或英文，播放时歌词列表会主动跟随当前行滚动。
- 歌词识别提示词幻觉已修正：移除 ASR 初始提示词并过滤指令类文本；对齐校正面板改为两行布局，歌词滚动增加后端信号强制跟随。
- 智能歌词模型默认从 base 切换到 small：优先使用 `plugins/models/faster-whisper/small`，缺失时回退 base；打包脚本会自动收集 small/base 本地模型目录。
- 导出已支持 MP3：UI 导出文件选择支持 `.mp3/.wav`，mp3 目标会先渲染临时 wav，再通过 ffmpeg 编码为 192k mp3。
- 歌词显示与校正继续修正：智能识别后的繁体兜底转换扩展到 `輪廓/看著/模樣` 等常见歌词词形；已唱过歌词改为缩小弱化；对齐校正中间新增 `0s 不校正`，并接入真实歌词偏移。
- 便携版导入目录已改为程序/批处理同级的 `导入歌曲`，默认扫描也指向该目录，避免每次打开后找不到已导入歌曲；左侧歌曲库新增“删除选中”和“清空列表”按钮。
- 滚动歌词当前句背景框改为按内容自适应宽度；底部播放模式按钮已接入真实逻辑，可在列表播放和单曲循环之间切换，歌曲结束后会自动切歌或重播。
- 底部混音区文案已改为“人声音量 / 伴奏音量 / 主输出增益”，百分比和 dB 数值随滑杆实时显示，静音按钮改为中文。
- 默认保存目录改为英文 `save`，启动时自动扫描该目录；默认分离后端改为 Demucs 真实人声分离，快速预览明确标注为“不消人声”，避免误以为已完成真实伴奏/人声分离。
- 滚动歌词当前句时间标签改为放在高亮胶囊最右侧，并从真实歌词时间轴读取时间，不再按行号估算。
- 交付注意事项已补充：开发阶段可临时依赖本机 `D:\uv\venvs\audio_forge` 运行 Demucs；正式交付必须把 sidecar Python、Demucs、torch、ffmpeg 和模型 checkpoint 一起放入便携包，不能要求用户自行安装环境。
- Demucs 导入体验继续修正：真实分离时 UI 显示导入进度条和“可能需要几分钟”的阶段提示；Demucs 子进程失败时保留末尾错误详情，便于区分“还在跑”和“真正失败”。

## 2026-05-24 右栏功能推进计划

目标：右栏从“视觉装饰面板”逐步变成可用的状态与快捷操作区；已经接入的功能显示真实状态，暂未实现的功能明确置灰和说明。

第一批：

- F0 音高监视器显示当前跑调强度、当前播放采样率和真实状态文案。
- “分离与重组算法预设”改成可点击入口：标准人声=快速预览，极致消除=Demucs 人声分离，影响下一次导入。
- 右栏底部显示真实输出状态、上一次对齐检测延迟、VST 状态和智能歌词状态。
- “AI 自动修补原唱”保持置灰，标注为后续实验功能，避免用户误以为已经生效。

状态：已完成第一批，见 `docs/开发记录/2026-05-24-右栏功能面板第一批.md`。

第二批：

- 增加右栏快捷诊断按钮：检测对齐、刷新设备、生成歌词。
- 将导出格式、当前插件链、当前歌曲工程状态放入右栏。

第三批：

- 如后续确定可用的开源音频修复/人声增强方案，再接入“AI 自动修补原唱”。
