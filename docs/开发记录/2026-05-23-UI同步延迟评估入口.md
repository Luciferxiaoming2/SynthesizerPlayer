# 2026-05-23 UI 同步延迟评估入口

## 本次目标

把已有 eval harness 的音频同步延迟检测能力接入 UI，让用户在界面中可以快速检查当前 vocal/instrumental 是否存在明显不同步。

## 已完成

- 扩展 `ui/main_window.py`：
  - 新增 `evaluateAlignment()`。
  - 调用 `harness.eval_harness.audio_latency_test.evaluate_latency`。
  - 将延迟毫秒数、相关性分数和通过/需检查状态写入状态栏。
- 扩展 `ui/qml/SingleScreenUI.qml`：
  - 新增 `Check` 按钮。
- 扩展 `tests/test_ui_workbench.py`：
  - 覆盖 UI bridge 调用同步评估。

## 本机验证

- UI bridge 同步评估测试通过。
- UI smoke 加载成功。

## 当前边界

- 当前评估是数学同步检查，不替代人工试听。
- 对完全不同内容的 vocal/instrumental，相关性分数可能偏低，需要结合素材类型解释。
