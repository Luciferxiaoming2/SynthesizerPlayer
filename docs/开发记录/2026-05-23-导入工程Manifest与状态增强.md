# 2026-05-23 导入工程 Manifest 与状态增强

## 本次目标

继续推进真实歌曲导入体验：让导入结果不仅生成文件，还能在工程 manifest 和 UI 状态中说明本次使用了哪些后端，方便后续客户样本排查和打包交付验证。

## 已完成

- 扩展 `core_engine/importer/song_importer.py`：
  - `SongImportConfig` 新增 `separator_backend`。
  - `SongImportConfig` 新增 `lyrics_backend`。
  - `project.json` 新增 `separator_backend`、`lyrics_backend`、`standardized_audio`、`copied_source`。
- 扩展 `harness/cli_harness/import_song.py`：
  - CLI 导入时把当前 `--backend` 和 `--lyrics-backend` 写入 manifest。
- 扩展 `ui/main_window.py`：
  - UI 导入完成后状态栏保留工程目录、分离后端和歌词后端。
- 扩展测试：
  - 覆盖 manifest 中的后端与标准化字段。
  - 覆盖 UI 导入状态包含后端信息。

## 本机验证

- `D:\uv\venvs\audio_forge\Scripts\python.exe -m pytest`
  - 65 passed
- `D:\uv\venvs\audio_forge\Scripts\python.exe scripts\validate_architecture.py`
  - Architecture validation passed
- `import_song --standardize-audio --lyrics-backend preview`
  - 成功生成工程。
  - `project.json` 正确记录 `separator_backend`、`lyrics_backend` 和 `standardized_audio`。

## 当前边界

- manifest 仍是本地工程描述，不承担版本迁移或数据库职责。
- UI 仍是同步导入，真实长音频 Demucs 处理时会阻塞界面；下一步需要做后台任务或至少更明确的进度/忙碌状态。
