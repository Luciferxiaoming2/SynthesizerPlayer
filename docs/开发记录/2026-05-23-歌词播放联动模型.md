# 2026-05-23 歌词播放联动模型

## 本批目标

推进进度跟踪中的 P1：播放位置与歌词状态联动。

## 完成内容

- 在 `core_engine/lyrics/timeline.py` 中新增：
  - `line_progress`
  - `__len__`
  - `__bool__`
- 新增 `core_engine/lyrics/playback_sync.py`：
  - `LyricPlaybackState`
  - `LyricPlaybackSynchronizer`
- 新增 `harness/cli_harness/lyrics_at.py`：
  - 可输入歌词文件、播放时间、offset，输出当前歌词状态。

## 状态对象包含

- 当前播放时间
- offset
- 当前歌词行索引
- 当前歌词行
- 上一句
- 下一句
- 当前文本
- 当前行内进度

## 验证覆盖

- 播放位置在第一句之前。
- 当前行、上一行、下一行。
- offset 偏移。
- SRT 结束时间用于行内进度。
- 空歌词。

## 验证命令

```powershell
D:\uv\venvs\audio_forge\Scripts\python.exe -m pytest -q
D:\uv\venvs\audio_forge\Scripts\python.exe scripts\run_agent_checks.py
```

