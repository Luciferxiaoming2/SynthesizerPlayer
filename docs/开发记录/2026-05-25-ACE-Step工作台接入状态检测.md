# 2026-05-25 ACE-Step 工作台接入状态检测

## 背景

ACE-Step 1.5 已能在 Intel 轻薄本上以 CPU 模式启动 Gradio 工作台，监听地址为 `http://127.0.0.1:7860`。当前运行日志显示无独显可用，会走 CPU 推理，适合作为真实 AI 改唱的实验通道。

## 本次改造

- 应用右侧“AI 智能改唱工作舱”新增 ACE-Step Web 工作台状态检测。
- 当 `http://127.0.0.1:7860/config` 可访问时，界面显示“ACE-Step Web 工作台已运行”。
- 新增“打开 ACE 工作台”和“刷新状态”按钮，方便用户从桌面应用跳转到 ACE 页面测试 Repaint/改写片段。
- 应用内“启动智能改唱并修补”暂时仍保持轻量预览路径，避免在 Gradio API 自动化未验证前误触长时间 CPU 推理。

## 验收

- `tests/test_ui_workbench.py` 新增 ACE 工作台状态检测测试。
- `tests/test_ui_workbench.py` 与 `tests/test_ai_singer_workflow.py` 共 71 条测试通过。
