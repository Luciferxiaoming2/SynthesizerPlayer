# 2026-05-25 ACE-Step v1.5 模型接入

## 当前进度

- 已检测并接入模型目录：`plugins/models/ace-step/acestep-v15-sft`
- 当前模型文件完整性要求：
  - `model.safetensors`
  - `config.json`
  - `configuration_acestep_v15.py`
  - `modeling_acestep_v15_base.py`
  - `silence_latent.pt`
- 应用启动后会自动识别该目录，并在右侧改词唱工作舱显示 `ACE-Step v1.5 歌词编辑`。
- 当前开发环境尚未安装 `acestep` 和 `diffusers`，因此不会直接调用真实推理，避免在轻薄本上误触发卡死或生成异常音频。
- 运行环境未就绪时，生成改词只更新歌词文本，不替换人声音频。

## 检查命令

```powershell
cd D:\project\code\audio_forge
D:\uv\venvs\audio_forge\Scripts\python.exe scripts\ai_singer\check_ace_step_model.py
```

## 后续

后续需要在可承受推理负载的环境中安装 ACE-Step 运行库，并确认官方 lyric editing API 的稳定调用方式。确认后再把真实命令接入 `edit_command`，最终打包时再决定是否将 4.79GB 模型随包分发。
